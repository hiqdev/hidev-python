default:
    before:
        - LICENSE
        - README.md
        - CHANGELOG.md
        - .travis.yml
        - .scrutinizer.yml
install:
    before:
        - LICENSE
check:
    before:
        - LICENSE
test:
    before:
        - install
build:
    before:
        - check
        - test

readme:
    badges:
        github.version:
        scrutinizer.coverage:
        scrutinizer.quality:

.travis.yml:
    language: python
    python:
        - 2.7
        - 3.4
    matrix:
        allow_failures:
            - python: 3.4

.scrutinizer.yml:
    checks:
        python:
            code_rating: true
            duplicate_code: true
    tools:
        external_code_coverage:
            timeout: 600
            runs:    1
